# تقرير إصلاحات 2026-09-24 — البيع، الاستطبابات، البدائل

ملاحظات علي الخمسة من نفس اليوم، كلها مُصلحة ومُتحقق منها أدناه.

## 1. «بند القيد بدون مبلغ» عند إدخال المبلغ المقبوض

**السبب الجذري:** المشكلة لم تكن في المبلغ المقبوض نفسه. عند بيع صنف بلا
تكلفة مسجلة (تكلفة صفر — مثل أصناف الكتالوج المستوردة بلا دفعة شراء)، كان
`postSale` ينشئ سطري تكلفة بضاعة/مخزون بمبلغ صفر، فيرفضهما مدقق القيد
برسالة «بند القيد بدون مبلغ» ويُجهض البيع كله عند شاشة الدفع.

**الإصلاح** (`lib/domain/services/financial_posting_service.dart`):
- `postSale` يتخطى زوج COGS/المخزون عندما `costMicros == 0`، ويبقى القيد
  متوازناً على الإيراد مقابل النقد/البنك/الذمم فقط.
- الحماية نفسها طُبقت على `postReturn` و`postVoidReversal` (المرتجع والإلغاء
  كانا سيُجهضان بالرسالة نفسها).
- البيع لا يُمنع أبداً بسبب غياب بيانات التكلفة.

**اختبارات انحدار** (`test/financial_lifecycle_test.dart`): بيع بتكلفة صفر،
مرتجع بتكلفة صفر، إلغاء بيع بتكلفة صفر — كلها تنشر قيوداً متوازنة بلا سطور
تكلفة.

## 2. زر التبديل الصغير للبيع بالجزء لا يعمل (الهاتف)

**السبب الجذري:** نافذة السلة السفلية في الهاتف كانت تستلم لقطة جامدة
(snapshot) من حالة السلة لحظة فتحها. أي تغيير بعده (تبديل عبوة/جزء، كمية،
حذف) كان يُنفذ في المتحكم لكن النافذة لا تُعاد رسمها، فيبدو الزر ميتاً.

**الإصلاح** (`lib/features/sales/presentation/pages/pos_workspace_page.dart`):
- `_CartPanel` أصبح يراقب `posWorkspaceControllerProvider(tabIndex)` مباشرة
  بدل اللقطة الجامدة — يعمل في سطح المكتب والنافذة السفلية معاً.
- `showPosCartSheet` تستقبل `tabIndex` بدل حالة مجمدة.

## 3. الاستطبابات

**نتيجتان منفصلتان:**

أ) **تلوث بيانات الاستيراد** — عُثر في الكتالوج على قيمة ملوثة مؤكدة
(`621704002572-4#6217040025724` — باركود ملتصق بعمود الاستطباب). التحصين
(`tool/convert_zena_catalog.py` و`inventory_excel_service.dart`): أي قيمة
استطباب تحتوي `#` أو تصبح أرقاماً فقط بعد إزالة المسافات/الشرطات تُحذف.
اختبار انحدار `test/inventory_indications_per_item_test.dart` يثبت أن كل صنف
مستورد يحتفظ باستطباباته هو فقط.

ب) **سوء فهم واجهة** — الرقائق المعروضة في نافذة الصنف هي قائمة الاختيار
الكاملة (كل الاستطبابات الرئيسية)، وليست روابط الصنف. المحدد منها فقط هو
المرتبط. أُضيف عداد للعنوان: «الاستطبابات (N)».

**ملاحظة لعلي:** القيمتان التالفتان في قاعدة بياناتك (`0` والباركود) دخلتا
قبل التحصين — احذفهما من شاشة البيانات الرئيسية (الاستطبابات) فهما غير
مرتبطتين بأي صنف الآن.

## 4. البدائل

- زر البدائل في نتائج بحث POS كان **مخفياً عندما يكون الصنف نافداً** — وهو
  أهم وقت للبحث عن بديل. أصبح ظاهراً دائماً (المتوفر أولاً، والنافد بعده).
- نافذة بدائل مشتركة (`alternatives_dialog.dart`) تخدم POS والمخزون.
- **زر بدائل لكل صنف** في شاشة المخزون **وشجرة المواد** (يخضع لصلاحية
  `viewAlternatives`) — يفتح النافذة للاستعراض، وفي POS يمكن اختيار البديل
  وإضافته للسلة مباشرة.
- اختبار `test/smart_alternatives_import_test.dart`: أصناف مستوردة بمواد
  فعالة علاقية تُنتج بدائل صحيحة عبر المستودع الحقيقي.

## التحقق

- `flutter analyze`: لا مشاكل.
- 106 اختبارات موجهة (الملفات المتأثرة): ناجحة.
- المجموعة الكاملة: 659 اختباراً ناجحاً.

---

# دفعة التحسينات التسعة — 2026-09-24 (مساءً)

طلب علي تسعة تحسينات، كلها نُفذت محلياً ومُتحقق منها أدناه.

## 1. قوائم منسدلة متعددة الاختيار (بحث + إضافة) في نافذة الصنف

**الملف الجديد:** `lib/core/widgets/searchable_multi_select_field.dart`
- ودجت عام يدعم البحث الفوري، الاختيار المتعدد، chips للمحدد، إلغاء الاختيار، عداد المحدد، إضافة قيمة جديدة inline، وقائمة محدودة الارتفاع.
- دُمج مع الاستطبابات والمواد الفعالة والموردين في `item_dialog.dart`.
- `strength` مستقل لكل مادة فعالة، مع التخلص من الـcontroller عند إزالة المادة.
- إضافة مادة فعالة أو استطباب جديد تفتح نافذة master data والاسم مكتوب مسبقاً.
- إضافة مورد جديد تفتح نافذة المورد وتُحدد المورد المُنشأ تلقائياً.

**اختبار:** `test/searchable_multi_select_field_test.dart` — اختباران ناجحان.

## 2. الشركة المصنعة والشكل الدوائي في تلميحات الأدوية

- `items_tab.dart` + ملفات الترجمة: التلميح الآن يعرض «الرصيد الحالي · سعر البيع · الشركة · الشكل الدوائي».

## 3. توحيد كلفة العبوة

- كل عروض الكلفة المباشرة في `items_tab.dart` تستخدم `baseUnitCostToPackageCost()` من `lib/core/units/package_cost.dart` (النقطة الوحيدة للتحويل، تقريب half-up).
- تقرير المخزون يحوّل كلفة الوحدة الأساسية إلى كلفة العبوة للعرض؛ قيمة المخزون الإجمالية ما زالت محسوبة بالوحدات الأساسية دون ضرب مزدوج.

## 4. إصلاح البدائل: مطابقة متدرجة بالألوان + نسبة + حالة المخزون

**البيانات:** أُضيف `relationalIngredientStrengths` إلى `PosCatalogItem`، و`pos_catalog_dao.dart` يحمّل عيار كل مادة من `item_active_ingredients.strength`. أُزيل الاقتطاع الأبجدي القديم قبل الترتيب.

**التصنيف** (`smart_alternatives_service.dart` + `smart_alternative.dart`):
- 🟢 أخضر / 100%: نفس مجموعة المواد ونفس العيار لكل مادة.
- 🟡 أصفر: نفس مجموعة المواد مع اختلاف في العيار (يُعرض الفرق).
- 🔵 أزرق: اشتراك جزئي — مواد زائدة أو ناقصة (تُعرض بالاسم).
- النسبة: المادة+العيار المتطابقان = 1.0، المادة المتطابقة بعيار مختلف = 0.6، المقام = أكبر عدد مواد بين المطلوب والبديل.
- الترتيب: المتوفر أولاً، ثم النسبة، ثم الدرجة، ثم كمية المخزون، ثم نفس الشركة، ثم الاسم.

**إزالة حد الـ200:** كان `alternativeCandidates` يقتطع النتائج عند 200 بدون ترتيب، مما قد يحجب بدائل صحيحة. أُزيل الحد — المرشحون مفلترون أصلاً بالمواد/الاستطبابات المشتركة فالمجموعة محدودة طبيعياً وكل بديل ذي صلة يُرتب.

**الواجهة:** `alternatives_dialog.dart` يعرض النسبة، شارة متوفر/غير متوفر، اختلافات العيار، والمواد الزائدة/الناقصة بالألوان الثلاثة.

## 5. سعر البيع لكل سطر في سلة المبيعات

- `pos_workspace_page.dart`: سطر السلة يعرض «الكمية الوحدة · سعر الوحدة» عبر `PosLinePricer().priceLine()` (محرك التسعير المعتمد).

## 6. قائمة مرتجعات واضحة ضمن المبيعات

**الجديد:**
- `lib/features/sales/domain/entities/pos_return.dart` — `PosReturnView` و`PosReturnLineView`.
- `lib/features/sales/presentation/pages/returns_list_page.dart` — بحث، بطاقات المرتجعات (الرقم، الفاتورة الأصلية، العميل، التاريخ، السبب، المبلغ، حالة «ملغي»)، ترقيم صفحات، ونافذة تفاصيل البنود والإجمالي.
- مسار `sales/returns` في `app_router.dart` + زر مرتجعات بارز بجانب سجل المبيعات.
- `SalesRepository.listReturns` / `returnDetail` — مرتجعات المبيعات فقط، بحث وترقيم داخل DB، الأحدث أولاً.

**اختبار تكامل** (`test/pos_integration_test.dart`): إنشاء صنف وفاتورة ومرتجع، والتحقق من القائمة والبحث والتفاصيل — ناجح.

## 7. إصلاح تجمد تقرير المخزون

- `inventory_report_tab.dart`: استُبدل بناء كل `DataRow` دفعة واحدة بـ`PaginatedDataTable` (25 صفاً/صفحة).

## 8. تسريع البحث في المخزون والشجرة والمبيعات

**الجديد:** `lib/core/search/item_search_text.dart` + عمود `search_text` (nullable) في `items` — schema ارتفع من 13 إلى 14.
- Migration v14 يتحقق من وجود العمود عبر `pragma_table_info` ثم يعيد حساب النص لكل الأصناف.
- البحث الأساسي عبر `items.searchText LIKE` مع fallback للصفوف القديمة.
- استعلامات Smart Search المساندة تعمل بالتوازي عبر `Future.wait`.
- **Benchmark** (20 ألف صنف): «بانادول» 67ms، باركود 11ms، بحث عام 50 نتيجة 141ms.

## 9. الشركة والشكل الدوائي في القائمة السريعة

- أُضيف الشكل الدوائي إلى `_quickSections` في `item_dialog.dart` (الشركة كانت موجودة)، وأُزيل حقل الشكل المكرر من القسم التفصيلي.

## تحديثات اختبارات مصاحبة

- `test/item_form_ux_test.dart`: حُدّث للواجهة الجديدة (النقر على حقل البحث، اختيار checkbox، إلخ) — 8 اختبارات ناجحة.
- `test/pos_workspace_page_test.dart`: حُدّث توقع «1 علبة» إلى `textContaining` لأن السطر يعرض السعر الآن — 7 اختبارات ناجحة.
- `lib/features/backup/domain/entities/backup_manifest.dart`: `kCurrentSupportedSchemaVersion` من 13 إلى 14 (schema الجديد).

## التحقق النهائي

- `flutter analyze`: **No issues found!**
- `flutter test`: **665 اختبار — كلها ناجحة.**

---

## الدفعة الثالثة — الميزات الذكية والتخصيص (2026-09-24)

### 1. ترقيم فواتير المبيعات اليومي التسلسلي

- الصيغة الجديدة: `S<YYMMDD>-<seq>` — مثال: `S240926-1`، `S240926-2`.
- التنفيذ في `PosCatalogDao.nextInvoiceNumber()`:
  - يبني بادئة اليوم من `DateTime.now()`.
  - يقرأ فواتير اليوم التي تبدأ بالبادئة ويستخرج أكبر suffix رقمي.
  - يتجاهل الأرقام القديمة/غير المطابقة (`SI-…`، `RT-…`).
  - يعيد التسلسل إلى 1 في اليوم التالي.
- `SalesRepositoryImpl` صار يستدعي النسخة الـ async من الـ DAO.
- **اختبارات** (`test/invoice_numbering_test.dart`): 4 اختبارات — أول فاتورة = 1، التزايد، تجاهل الأرقام القديمة، يوم جديد.

### 2. تنبيهات النواقص الذكية حسب سرعة البيع

- ملفات جديدة:
  - `lib/features/reports/domain/entities/reorder_suggestion.dart`
  - `lib/features/reports/domain/services/reorder_suggestions_service.dart`
  - `lib/features/reports/presentation/pages/reorder_suggestions_page.dart`
- يحلل مبيعات آخر 30 يوماً (من `salesInvoiceItems` غير الملغاة).
- يحسب متوسط البيع اليومي بالوحدات الأساسية ويجمع المخزون من الدفعات غير الملغاة.
- التصنيف:
  - **حرج**: مخزون صفر مع مبيعات، أو تغطية أقل من 7 أيام.
  - **قريب النفاد**: تغطية أقل من 14 يوماً.
  - **تحت الحد الأدنى**: عند انخفاض سرعة البيع وتجاوز `minimumStockBase`.
- الكمية المقترحة تهدف لتغطية 30 يوماً.
- **اختبارات** (`test/smart_alerts_test.dart`): 3 اختبارات للخدمة.

### 3. لوحة تواريخ الصلاحية

- ملفات جديدة:
  - `lib/features/reports/domain/entities/expiry_alert.dart`
  - `lib/features/reports/domain/services/expiry_alerts_service.dart`
  - `lib/features/reports/presentation/pages/expiry_alerts_page.dart`
- يعرض الدفعات غير الملغاة ذات الكمية الموجبة ضمن أفق 180 يوماً.
- التصنيف: **منتهي** / **حرج** (≤90 يوم) / **قريب الانتهاء** (≤180 يوم).
- مرتب حسب الأقرب/الأكثر تجاوزاً أولاً، مع اسم الصنف ورقم الدفعة والرصيد.
- **اختبارات**: ضمن `test/smart_alerts_test.dart` — تصنيف الدفعات والترتيب.

### 4. سجل تغيّر أسعار البيع

- ملف جديد: `lib/features/reports/presentation/pages/price_history_page.dart`.
- يقرأ `audit_logs` حيث `action = price_change` (يسجلها `UpdateItemUseCase` ومسار الشراء).
- يعرض: اسم الصنف، المستخدم، التاريخ، والتغييرات قبل/بعد في:
  - سعر البيع، سعر الجزء، الجملة، نصف الجملة، السعرين الخاصين، والكلفة.
- بحث نصي في الملاحظات/الأسماء.

### 5. ربط الصفحات الثلاث بالتقارير

- أُضيفت كـ tabs في `ReportsHubPage`:
  - اقتراحات إعادة الطلب.
  - تنبيهات الصلاحية.
  - سجل تغيّر الأسعار.
- الصفحات حُوّلت من `Scaffold` إلى محتوى tabs (بدون AppBar مكرر).

### 6. لوحة التحكم الصباحية

- **موجودة أصلاً** (`lib/features/dashboard/`) — لم تُبنَ من جديد:
  - بطاقات KPI: مبيعات اليوم، الأرباح، المخزون، العملاء.
  - تنبيهات: الأصناف منخفضة المخزون، الدفعات قريبة الانتهاء.
  - آخر فواتير المبيع والشراء.

### 7. كثافة العرض (مريح/مضغوط)

- ملف جديد: `lib/features/settings/application/ui_preferences_service.dart`.
- التخزين في `app_settings`:
  - `ui.display_density` = `comfortable` | `compact`.
- `displayDensityProvider` (StateNotifier) يقرأ/يكتب الإعداد.
- `AppDataTable` يقبل `dataRowHeight` اختيارياً.
- جدول الأصناف (`items_tab.dart`) يطبق الارتفاع حسب الكثافة (48 مقابل 64).
- واجهة الاختيار في الإعدادات: `SegmentedButton` مريح/مضغوط.

### 8. تخصيص قالب الإيصال

- إعدادات جديدة في `app_settings`:
  - `receipt.pharmacy_name` — اسم الصيدلية في الإيصال.
  - `receipt.promo_line` — سطر إعلاني اختياري.
  - `receipt.font_size` — حجم الخط (8–14).
- واجهة في الإعدادات: حقول نصية + Slider لحجم الخط.
- `ReceiptPdfService.buildBytes/print` تقبل `promoLine` و`fontSize`.
- نافذة الطباعة في POS تقرأ الإعدادات وتمررها.

### 9. شرح الاختصارات في الإعدادات

- قسم «شرح الاختصارات» في صفحة الإعدادات يعرض:
  - `F1`: التركيز على حقل البحث في نقطة البيع.
  - `F2`: تبديل وحدة السطر بين العلبة والجزء.
  - `F5`: تعليق الفاتورة الحالية مؤقتاً.
  - `F12`: الانتقال إلى الدفع وإتمام الفاتورة.
  - `Alt+S`: فتح نافذة البدائل الذكية للصنف المحدد.
  - `Enter`: في البحث — إنهاء قراءة الباركود أو إضافة النتيجة الوحيدة.
- واجهة إعادة الربط الحالية محفوظة كما هي.

## ملاحظات تقنية

- **ترجمات**: أُضيفت ~37 مفتاحاً جديداً في `app_ar.arb` و`app_en.arb`، وشُغّل `flutter gen-l10n`.
- **لا تغيير في schema** — كل الميزات تستخدم الجداول الموجودة.
- **لا تكرار لمنطق التكلفة** — لم يُمس `lib/core/units/package_cost.dart`.

## التحقق النهائي

- `flutter analyze`: **No issues found!**
- `flutter test`: **673 اختبار — كلها ناجحة (665 سابقة + 8 جديدة).

---

# دفعة 2026-09-25 — ملاحظات علي الست

## 1. السكرول الأفقي للقوائم العريضة
- `AppDataTable` (مستخدم في ~20 شاشة) كان فيه تمرير أفقي لكن بدون Scrollbar ظاهر — أُضيف `Scrollbar` ليكون واضحاً.
- `PaginatedDataTable` في تقرير المخزون لم يكن ملفوفاً بتمرير أفقي — أُضيف `SingleChildScrollView` أفقي.

## 2. سجل المبيعات — تفاصيل الفواتير قابلة للتوسيع
- أُعيد تصميم `sales_history_page.dart`: بدل الجدول الثابت، كل فاتورة بطاقة قابلة للتوسيع.
- الضغط على الفاتورة يعرض سطورها (المواد المباعة + الكميات + الأسعار) مباشرة في القائمة.
- زر لفتح صفحة التفاصيل الكاملة.
- يعرض كل الفواتير بترتيب زمني (الأحدث أولاً) بدون الحاجة للبحث — مفيد للمرتجعات حيث لا يحفظ الصيدلي رقم الفاتورة.

## 3. إصلاح تدفق: إضافة مادة → مخزون → فاتورة شراء
- نافذة الدفعة (`batch_dialog.dart`) الآن تأخذ سعر التكلفة الافتراضي من المادة (محولاً لسعر العبوة عند الإدخال بالعبوات).
- `batches_page.dart` يمرر `initialCostMicros` من المادة.
- فاتورة الشراء (`_applyPrefill`) الآن تدخل الكمية بالعبوات دائماً (وليس بالشريط/الجزء) عندما يكون للمادة عبوة — الجزء للبيع الجزئي في نافذة البيع فقط.

## 4. فاتورة الشراء — استلام فوري أو طلبية معلقة
- أُضيف `SegmentedButton` في نافذة فاتورة الشراء الجديدة: «استلام فوري» (افتراضي) أو «حفظ كطلبية معلقة».
- الاستلام الفوري ينشئ الفاتورة ويستلمها مباشرة (تُضاف للمخزون) بأرقام تشغيلات تلقائية.
- `PurchasesController.createAndReceive()` جديد؛ `PurchasesRepository` أُضيف لحقن الـ Controller.

## 5. البدائل — فتح نافذة المنتج
- أُضيف زر معلومات (ⓘ) بجانب كل بديل في `alternatives_dialog.dart`.
- يفتح نافذة `ProductDetailDialog` (جديدة) تعرض بيانات المنتج: الاسم التجاري/الإنجليزي/العلمي، الباركود، سعر البيع، التكلفة، المخزون.

## 6. تقرير المخزون — فقط ما له مخزون فعلي
- استعلام `inventoryReport` في `reports_dao.dart` أُضيف له `HAVING SUM(b.quantity_base) > 0`.
- الشجرة (كتالوج السوق) لم تعد تُعتبر مخزوناً — التقرير يعرض فقط الأصناف ذات الكمية الموجبة في الدفعات.

## ملاحظات تقنية
- **ترجمات**: أُضيفت مفاتيح جديدة (استلام فوري، حفظ كطلبية معلقة، بيانات المنتج، إلخ) في `app_localizations.dart` والملفين العربي والإنجليزي.
- **لا تغيير في schema**.
- **لم يُشغّل `flutter analyze` أو `flutter test`** — بيئة العمل الحالية لا تحتوي Flutter. يُنصح بالتشغيل بعد السحب.

## إصلاحات CI (2026-09-25)
- فشل `flutter analyze` أولاً: الترجمات الجديدة كانت مضافة يدوياً لملفات مولّدة يمحوها `flutter gen-l10n` — نُقلت إلى `app_ar.arb` و`app_en.arb`.
- ثم فشل لسبب `PurchasesController`: معامل `_repo` الإجباري كسر اختبارين — صار `purchasesRepository` اختيارياً مسمّى.
- ثم `Failure.validation` غير موجود — استُبدل بـ `ValidationFailure`.
- ثم تحذير import غير مستخدم في `product_detail_dialog.dart`.
- ثم اختبار `sales_history_page_test`: بطاقة الفاتورة الجديدة دمجت اسم العميل مع التاريخ — فُصلت النصوص (العميل، الكاشير، التاريخ، طريقة الدفع) لتظهر مستقلة.
- التحقق المحلي: `flutter analyze` نظيف، `flutter test`: **673 اختبار ناجح**.

# تقرير إصلاحات 2026-09-25 — البدائل، التمرير الأفقي، ملاءمة الشاشة، المرتجعات

ملاحظات علي الأربعة، كلها مُصلحة ومُتحقق منها أدناه.

## 1. البدائل — النقر يفتح نافذة الصنف في المخزون/الشجرة

**المطلوب:** عند الضغط على صنف بديل في نافذة البدائل، تُفتح نافذة الصنف في
المخزون أو شجرة الأصناف (وليس نافذة عامة)، مع تولتيب يعرض الشركة المصنعة
والمواد الفعالة.

**الإصلاح** (`alternatives_dialog.dart`, `items_tab.dart`):
- صفوف البدائل في وضع التصفح صارت قابلة للنقر (`onTap`).
- أُضيف `onOpenItem` لنافذة البدائل: عند النقر يستدعي المنادي (المخزون/الشجرة)
  فيفتح نافذة الصنف الحقيقية (نافذة التعديل `_editItem`) — بعد إغلاق نافذة
  البدائل أولاً.
- إذا كان الصنف في الصفحة الحالية يُستخدم مباشرة، وإلا يُحمَل من المستودع
  (`findItem` + `itemUnitsFor`) — لأن البديل قد يكون في صفحة أخرى.
- المستخدم بلا صلاحية تعديل يرى نافذة العرض القراءة فقط (`ProductDetailDialog`).
- وضع الكاشير لم يتغير: النقر يضيف للسلة (`onPick`).
- التولتيب يعرض: الشركة المصنعة + المواد الفعالة العلائقية (مع العيارات) أو
  المادة الفعالة النصية كاحتياطي.

**اختبارات انحدار** (`test/alternatives_dialog_test.dart` — 4 اختبارات):
- النقر في وضع التصفح يستدعي `onOpenItem` بمعرف الصنف الصحيح ولا يفتح النافذة العامة.
- بدون `onOpenItem` يفتح نافذة العرض (احتياطي).
- التولتيب يعرض الشركة والمواد والعيارات.
- وضع الكاشير ما زال يضيف للسلة.

## 2. التمرير الأفقي — إصلاح السبب الجذري

**المشكلة:** القوائم العريضة كانت بلا شريط تمرير أفقي، فالأزرار يمين الجدول
كانت خارج الشاشة ولا يمكن الوصول إليها.

**السبب الجذري:** `SingleChildScrollView` الأفقي كان بلا `ScrollController`
متصل بشريط تمرير — فلم يظهر شريط ولم يعمل السحب.

**الإصلاح** (`horizontal_scroll.dart` جديد، `app_data_table.dart`):
- مكون `HorizontalScroll` جديد: `ScrollController` واحد صريح متصل بالشريط
  (`Scrollbar`) وبـ `SingleChildScrollView` معاً، الشريط ظاهر دائماً.
- `AppDataTable` صار `StatefulWidget` يملك متحكم التمرير العمودي ويستخدم
  `HorizontalScroll` للأفقي.
- `inventory_report_tab.dart`: استُبدل التمرير الأفقي بلا متحكم بـ `HorizontalScroll`.

**اختبار انحدار** (`test/horizontal_scroll_test.dart` — 2 اختبارات):
- يثبت حركة أفقية فعلية: سحب ثلاث مرات يغيّر إزاحة المتحكم ويُدخل العمود
  الأقصى يمين الشاشة.
- يتحقق من اتصال الشريط بالمتحكم وظهور المؤشر.

## 3. كل النوافذ والقوائم بحجم الشاشة

**المطلوب:** لا تمرير أفقي ولا عمودي — إعادة تصميم الحقول والنوافذ والصناديق
لتلائم الشاشة، مع ترقيم الصفحات للبيانات الكبيرة.

**ما تم:**

### أ. لوحة التحكم — بلا تمرير نهائياً
- أُعيد تصميم `dashboard_page.dart`: عمود ثابت بلا `ListView`.
- بطاقات KPI مدمجة + شريط مالي مدمج + صفّان من بطاقتي تنبيه/نشاط.
- كل بطاقة تحسب عدد الصفوف التي تتسع لها (بحد أقصى 4).
- روابط «عرض الكل» تنقل للمخزون/سجل المبيعات/المشتريات.
- **اختبار** (`test/dashboard_page_test.dart`): يتحقق عند 1280×900 و1024×640
  و800×600 من غياب أي `Scrollable` وغياب التجاوز.

### ب. نافذة الصنف — تبويبات بدل التمرير الطويل
- وضع «تفصيلي» انقسم لأربعة تبويبات ثابتة ظاهرة دائماً:
  البيانات الأساسية / المواد والجهات / التسعير والمخزون / ملاحظات.
- التبويبات غير قابلة للتمرير (كلها ظاهرة وقابلة للنقر).
- أزرار الحفظ/الإلغاء مثبتة دائماً في الأسفل.
- **اختبارات** (`test/item_form_ux_test.dart` — 8 اختبارات) حُدثت للتنقل بين التبويبات.

### ج. قائمة المخزون/الشجرة — ترقيم صفحات متكيف مع الشاشة
- حجم الصفحة لم يعد ثابتاً (30): يُحسب من الارتفاع المتاح.
- الجدول (صفوف بارتفاع ثابت 64/48 حسب الكثافة): عدد الصفوف = المتاح ÷ الارتفاع بدقة.
- البطاقات (الهواتف): تقدير 152px للبطاقة.
- `InventoryController.load()` يقبل `pageSize` ويحافظ عليه عبر البحث والفلاتر وتقليب الصفحات.
- مساعد مشترك `rowsThatFit()` في `lib/core/widgets/adaptive_page_size.dart`.
- **اختبارات** (`test/adaptive_page_size_test.dart` — 6 اختبارات).

### د. النوافذ الأخرى
- نوافذ البدائل (460px) وتفاصيل المنتج (480px) والدفعات مدمجة أصلاً وتتسع للشاشة.
- شاشة الكاشير متجاوبة أصلاً (`AppResponsiveLayout`: سطح مكتب/لوحي/هاتف).

**استثناءات موثقة (تمرير داخلي لا مفر منه):**
- محتوى تبويبات نافذة الصنف: يمرر داخلياً فقط إذا تجاوز المحتوى الشاشة (شبكة الأمان)،
  والأزرار مثبتة دائماً.
- قائمة البدائل داخل نافذتها: قد تمرر عمودياً عند كثرة البدائل (النافذة نفسها بحجم الشاشة).
- سلة الكاشير: تمرر عمودياً بطبيعتها (قائمة سطور متغيرة).
- بطاقات الهواتف: تقدير الارتفاع قد يخطئ بهامش بسيط، والقائمة الداخلية شبكة أمان.

## 4. المرتجعات — تحميل تلقائي عند فتح الصفحة

**المطلوب:** المرتجعات/الفواتير تُحمَل تلقائياً عند فتح صفحة المرتجعات، والبحث يبقى اختيارياً.

**الإصلاح** (`returns_list_page.dart`):
- التحميل يتم في `initState` عبر `Future.microtask` — بلا أي نقر من المستخدم.
- إعادة محاولة واحدة تلقائية بعد 800ms إذا فشل التحميل الأول (قد يتسابق مع
  بدء التطبيق/فتح قاعدة البيانات) — ثم يعرض الخطأ إن تكرر الفشل.
- البحث ما زال يعمل ويرسل الاستعلام المدخل.

**اختبارات انحدار** (`test/returns_list_page_test.dart` — 2 اختبارات):
- المرتجعات تظهر بعد الفتح مباشرة بلا تفاعل.
- البحث ما زال يرسل الاستعلام.

## التحقق
- `flutter analyze`: نظيف.
- `flutter test`: **688 اختبار ناجح** (14 جديد).
- CI: أخضر على `main`.

## ملاحظات تقنية
- **لا تغيير في schema** (v14).
- **ترجمات**: `commonViewAll` + مفاتيح التبويبات الأربعة في `app_ar.arb` و`app_en.arb`.
- **لا تغيير في منطق التكلفة** (`package_cost.dart` untouched).

---

## إعادة تصميم الواجهات لتتكيّف مع حجم الشاشة (2026-09-25)

بناءً على طلب علي: «بدي كلشي يتكيف مع حجم الشاشة ولو اضررينا لتغير الواجهات
وحجم الحقول» — أُعيد تصميم الحقول والحوارات والقوائم بالكامل بدل الاكتفاء
بتمرير أفقي/عمودي. الشكوى الأصلية: نافذة الصنف (`item_dialog.dart`) تُهدر
المساحة — حقول كبيرة داخل نافذة صغيرة.

### ما تغيّر

**مكوّنات مشتركة جديدة** (`lib/core/widgets/compact_form.dart`):
- `FormGrid`: شبكة حقول متجاوبة — عمودان على الشاشات العريضة، عمود واحد
  تلقائيًا على الهاتف، بلا أي تمرير أفقي.
- `CompactSection`: عنوان قسم بمسافات أضيق.

**كثافة عامة** (`app_theme.dart`): حشوة الحقول من 16/12 إلى 8/8 أفقي/عمودي —
يطبَّق على كل حقول التطبيق.

**الجداول** (`app_data_table.dart`): `columnSpacing: 20` و`horizontalMargin: 12`
بدل القيم الافتراضية المُبذِّرة — يقلّل التجاوز الأفقي.

**نافذة الصنف** (`item_dialog.dart`) — إعادة تصميم كاملة:
- كل مجموعات `Wrap` ذات العروض الثابتة (140–250px) أصبحت `FormGrid` متجاوبة.
- عرض النافذة وارتفاع التبويبات يتكيّفان مع الشاشة
  (`min(680, screenWidth - 64)` و`screenHeight - 340`) بدل 680/520 الثابتة.
- مفتاح «إدخال سريع/مفصّل» كان يفيض 152px على الهاتف — أُصلح بـ `Expanded`
  وإسقاط أيقونات المقاطع عند ضيق المساحة.
- صفوف المواد الفعالة أصبحت `Expanded` بنسب بدل عروض ثابتة.
- زرّا الوضع والحفظ دائمًا مرئيان بلا تمرير.

**حوارات أُعيد تصميم تخطيطها**: الدفعة (`batch_dialog`)، تسوية المخزون
(`stock_adjust_dialog`)، البيانات الأساسية (`master_data_dialog`)، المورّد
(`supplier_dialog`) — كلها `FormGrid` بعرض متكيّف.

**حوارات البدائل وتفاصيل المنتج** (`alternatives_dialog`،
`product_detail_dialog`): عرض متكيّف، وقائمة البدائل بارتفاع محدود تتمرير
داخليًا مع بقاء زر الإغلاق ظاهرًا.

**عرض متكيّف (ميكانيكي) لبقية الحوارات**: الحسابات، القيد/الفترات، الصلاحيات،
الأدوار، العملاء، مدفوعات العملاء، المصاريف (4 حوارات)، الدفعات الجماعية،
مرتجع المشتريات، الاستلام — كلها `min(N, screenWidth - 64)`.

**صفحة فاتورة الشراء** (`purchase_form_page`): حوارا البحث عن صنف والمكافآت
بعرض/ارتفاع متكيّفَين، وصف المبلغ المدفوع/الملاحظات أصبح `Expanded` بنسب.

### اختبارات جديدة (`test/responsive_dialogs_test.dart` — 12 اختبارًا)

تغطي 1280×800 (مكتبي) و390×844 (هاتف): نافذة الصنف (سريع + التبويبات الأربعة)،
الدفعة، تسوية المخزون، المورّد، البدائل — بلا أي فيض تخطيط، والزر الرئيسي
(حفظ/إغلاق) مرئي بالكامل وقابل للنقر بلا تمرير. تحقّق أيضًا من سلامة تحويل
العبوات (5 علب × 10 = 50 وحدة أساس).

### التحقق
- `flutter analyze`: نظيف.
- `flutter test`: **700 اختبار ناجح** (12 جديد).
- CI: أخضر على `main` (يُتحقق بعد الدفع).

### ملاحظات تقنية
- **لا تغيير في منطق التكلفة** (`package_cost.dart` untouched) — التحويل
  نصف-للأعلى ما زال في نقطة واحدة.
- **لا تغيير في schema** (v14).
- ما لم يتّسع: محتوى تبويب «المواد والجهات» الطويل جدًا (عشرات المواد) يتمرير
  داخليًا ضمن التبويب — الأزرار تبقى ثابتة وظاهرة، وهذا مقصود وليس نقصًا.

---

## 2026-09-25 — إصلاح شاشة البدائل السوداء + تسريع البحث أثناء الكتابة

### 1) الشاشة السوداء عند النقر على بديل — السبب الجذري والإصلاح

**العرض:** النقر على أي بديل في حوار البدائل يحوّل الشاشة إلى سوداء ويجمّد
التطبيق، ولا يخرج إلا بالإغلاق القسري.

**السبب الجذري (مؤكّد بإعادة إنتاج في اختبار):** التطبيق يستخدم `ShellRoute`
في go_router، أي أن صفحة المخزون تعيش داخل Navigator متداخل، بينما
`showDialog` يفتح الحوار على الـ Navigator الجذري (`useRootNavigator: true`).
الكود القديم كان يغلق الحوار من الطرف المتصل بـ
`Navigator.of(context).pop()` حيث الـ context تابع لصفحة المخزون — أي أنه
كان يغلق صفحة المخزون نفسها بدل إغلاق الحوار، فيبقى حوار البدائل مفتوحًا
وتُفتح نافذة الصنف فوقه: مكدّس تنقّل مكسور = شاشة سوداء متجمدة. نفس النمط
الخاطئ كان موجودًا في مسار بدائل نقطة البيع.

**الإصلاح:**
- `alternatives_dialog.dart`: الحوار يُغلق نفسه بسياقه الخاص قبل استدعاء
  `onOpenItem` / `onPick` (الحوار يملك إغلاقه — عقد موثّق بتعليق).
- `items_tab.dart` و`pos_workspace_page.dart`: حُذف `pop()` الخاطئ من الطرف
  المتصل.

**اختبارات** (`test/alternatives_tap_navigation_test.dart`): بنية Navigator
متداخل مطابقة للإنتاج — النقر على بديل يطلق الـ callback مرة واحدة بالمعرّف
الصحيح، يُغلق حوار البدائل، تُفتح نافذة الصنف، وتبقى الصفحة المضيفة سليمة؛
ومسار بدائل نقطة البيع يُغلق الحوار ذاتيًا.

### 2) تسريع البحث أثناء الكتابة في نقطة البيع

**القياس قبل الإصلاح** (قاعدة بيانات اختبار: 10,000 صنف بأسماء عربية، نفس
مسار `PosCatalogDao.search` الكامل: عدّ + استعلام + ترطيب):
- حرف واحد «ب»: p50 ≈ **156ms** — حرفان «با»: ≈ **170ms** — كلمة «بانادول»:
  ≈ **165ms**.
- فوقها تأخير واجهة ثابت **650ms** (debounce) قبل بدء أي بحث → الإحساس
  الفعلي ≈ **800ms+** من الضغطة حتى النتائج.
- السبب: البحث كان يطبّق سلسلة `replace()` (تطبيع البحث الذكي) على **7 أعمدة
  × كل صف × كل ضغطة** مع `LIKE '%…%'`، رغم وجود عمود `search_text` المحسوب
  مسبقًا (schema v14) الذي لم يكن مسار نقطة البيع يستخدمه.

**ما تغيّر:**
1. `pos_catalog_dao.dart`: البحث يستخدم `items.search_text` (LIKE واحد على
   عمود عادي) مع fallback للصيغة القديمة للصفوف التي `search_text` فيها NULL
   (ما قبل v14) — نفس نهج `ItemDao` المعتمد (§search-perf). التطبيع
   (ألف/همزة/تاء مربوطة…) محفوظ عبر `ItemSearchText.normalizeQuery`.
2. `pos_workspace_page.dart`: الـ debounce من **650ms إلى 150ms** — النتائج
   تبدأ من الحرف الأول. دفعات الماسح تصل أسرع من ذلك بكثير وتنتهي بـ Enter
   (يُعالَج فوريًا عبر `_onSubmitted`) فلا تتأثر.
3. إلغاء الاستعلامات الطائرة (generation token): في
   `PosWorkspaceController.search` وفي `InventoryController.load` — ضغطة
   أحدث تُسقط نتائج أي بحث أقدم ما زال قيد التنفيذ، فلا تظهر نتائج قديمة فوق
   الأحدث ولا تتكدّس الاستعلامات. (في المخزون أُضيفت حماية إضافية: إسقاط
   التحديث المتقادم لا يترك مؤشر الانشغال عالقًا بعد عملية حفظ.)

**القياس بعد الإصلاح** (نفس قاعدة البيانات والمسار):
- «ب»: p50 ≈ **42ms** — «با»: ≈ **21ms** — «بان»: ≈ **18ms** — «بانادول»:
  ≈ **17ms** — «اوجمنتين» (ألف بدون همزة): ≈ **20ms**.
- الإحساس الفعلي الآن ≈ 150ms + ~40ms ≈ **200ms** من الضغطة حتى النتائج
  (مقابل ~800ms+ قبل).
- ملاحظة: لا يوجد فهرس على `search_text` — `LIKE '%…%'` لا يستفيد من فهرس
  B-tree أصلًا؛ المكسب من إزالة سلاسل `replace()` لا من الفهرسة. قياس
  `EXPLAIN QUERY PLAN` وثّق أن المسارين SCAN لكن بتكلفة دالة مختلفة جذريًا.

**اختبارات جديدة** (`test/pos_search_perf_test.dart` — 7 اختبارات):
- p50 < 150ms لحرف واحد و< 100ms لحرفين/كلمة على 10k صف.
- صحة النتائج عبر `search_text` (العدد، الصفحة، ترتيب تطابق بداية الاسم أولًا).
- تطابق متغيرات الألف/الهمزة، وfallback الصفوف ذات `search_text = NULL`.
- ضغطة ثانية تُسقط نتائج الأولى الطائرة (repository وهمي متحكَّم به)، ومسح
  الحقل يمسح النتائج فورًا ولا يعيد ملؤها استعلامٌ يتيم.

### التحقق
- `flutter analyze`: نظيف.
- `flutter test`: **709 اختبارات ناجحة** (700 سابقة + 2 تنقّل البدائل + 7
  أداء/إلغاء البحث).
- CI: يُتحقق بعد الدفع.

### ملاحظات تقنية
- **لا تغيير في منطق التكلفة** (`package_cost.dart` untouched).
- **لا تغيير في schema** (v14) — `search_text` كان موجودًا ومُعبّأً؛ التغيير
  استخدامُه فقط في مسار نقطة البيع.
- ترتيب الملاءمة (relevance) في ORDER BY ما زال بالصيغة القديمة (سلسلة
  replace على اسم الصنف للصفوف المُرشّحة فقط) — حُوفظ على سلوك الترتيب
  الحالي عمدًا؛ تكلفته ~25ms في أسوأ حالة (4000 صف مرشّح) وهي مقبولة.
- ما لم يُقَس بعد: زمن الاستجابة على جهاز حقيقي بقاعدة 22k صف (الملف) —
  الأرقام أعلاه من قاعدة اختبار بذاكرة 10k صف.

## 2026-09-25 — إصلاح حرج: قائمة الجرد تعلق على «تحميل» + 4-5 عناصر فقط بالصفحة

### المشكلة (تقرير علي على البناء `7f2ddb1`)
- فتح تبويب «المواد المخزنة» في قائمة الجرد: مؤشر التحميل لا ينتهي أبدًا،
  استهلاك الذاكرة والمعالج يرتفع، والجهاز يسخن — لا بد من إغلاق التطبيق قسرًا.
- القائمة تعرض 4-5 عناصر فقط بالصفحة — والمطلوب ~25 عنصرًا على الأقل.

### السبب الجذري (مؤكد بإعادة إنتاج حتمية)
1. **حلقة إعادة تحميل لانهائية**: `_syncPageSize` في `items_tab.dart` يعمل
   داخل `LayoutBuilder` عند كل بناء، ويجدول `load(pageSize: المحسوب)` عبر
   post-frame كلما اختلف المحسوب عن `state.request.pageSize`. لكن
   `InventoryController.load()` كان ينشر `request` (ومنها `pageSize`) **فقط
   عند اكتمال الاستعلام**، بينما يضبط `loading` فورًا — وكل `loading` يُعيد
   البناء، فيرى المزامِن القيمة القديمة ويجدول تحميلًا جديدًا **يُبطل سابقه**
   عبر generation token. على جهاز بطيء (قاعدة 22k صنف يستغرق استعلامها
   أطول من إطار واحد) لا يكتمل أي استعلام قبل أن يُستبدَل، فلا يلتزم أي
   تحميل أبدًا: المؤشر لا يختفي، والاستعلامات المُبطَلة تتراكم في الخلفية
   (CPU/RAM) — وهذا بالضبط ما رآه علي. على قاعدة اختبار سريعة كان
   الاستعلام يكتمل خلال إطار أو إطارين فينجو الاختبار السطحي.
2. **صفحة ضئيلة**: `rowsThatFit()` كان يحسب الملاءمة الهندسية الدقيقة
   بأرضية 4 — على نافذة 900px يعطي ~9 صفوف، وعلى نوافذ أصغر 4-5 كما رأى
   علي. وهندسيًا يستحيل عرض 25 صفًا بارتفاع 64px في نافذة عادية، فالمعنى
   الصحيح لـ«25 عنصرًا»: **25 عنصرًا بالصفحة** (اتفاقية التطبيق في كل
   القوائم: الحسابات، الصندوق، التدقيق، التقارير) مع تمرير داخلي عند
   الحاجة — لا 25 صفًا مرئيًا دفعة واحدة.

### الإصلاح
- `inventory_controller.dart`: `load()` يبني `PageRequest` مرة واحدة
  **وينشره في الحالة فور الدخول في `loading`** (لا عند الاكتمال فقط). بهذا
  ترى المزامنةُ الحجمَ المطلوب فتتوقف بعد تصحيح واحد — الحلقة انكسرت من
  جذرها، والفحص الاحترازي الموجود داخل post-frame أصبح فعالًا فعلًا.
- `adaptive_page_size.dart`: أرضية `rowsThatFit` أصبحت **25**
  (`kConventionalPageSize`) بدل 4 — الصفحة لا تقل عن 25 أبدًا، وتكبر مع
  النوافذ الطويلة (حتى 60). الجدول/البطاقات يملكان تمريرًا داخليًا كشبكة
  أمان، ومرقّم الصفحات يبقى ظاهرًا.
- `items_tab.dart`: تحديث التعليق التوثيقي فقط — لا تغيير سلوكي إضافي.

### اختبارات جديدة
- `test/inventory_list_loading_loop_test.dart` (4 اختبارات):
  - **جهاز بطيء حتمي** (تأخير استعلام 300ms يحاكي جهاز علي): على الكود
    القديم **42 استعلامًا** والحالة عالقة على `loading` (فشل كما هو متوقع —
    أعاد إنتاج العطل بدقة)؛ بعد الإصلاح استعلامان فقط (الأولي + تصحيح
    واحد) والحالة `ready`.
  - التحميل ينتهي بعدد محدود من الاستعلامات (≤3) على قاعدة سريعة.
  - حجم الصفحة على نافذة مكتبية 1280×900 هو **25**.
  - `rowsThatFit` مُثبَّت عند 25 ولا ينهار إلى 4-5.
- `test/adaptive_page_size_test.dart`: حُدّث لسياسة الأرضية الجديدة
  (7 اختبارات).

### التحقق
- `flutter analyze`: نظيف.
- `flutter test`: **714 اختبارًا ناجحًا**.
- CI: يُتحقق بعد الدفع.

### ملاحظات تقنية
- **لا تغيير في منطق التكلفة** (`package_cost.dart` untouched).
- **لا تغيير في schema**.
- البحث السريع، RTL العربية، وتصميم الواجهات — بلا تغيير.
- **يبقى للتأكيد على جهاز علي**: أن القائمة تفتح فورًا بلا تعليق، وأن
  الصفحة تحمل 25 عنصرًا.

## 2026-09-26 — إعادة تصميم نافذة «إضافة / تعديل صنف» (ثلاثة أعمدة)

### 1. الملفات المعدّلة
- `lib/features/inventory/presentation/widgets/item_dialog.dart` — إعادة بناء
  كاملة لواجهة النافذة (نفس الحالة، نفس المتحكمات، نفس التحقق، نفس الحفظ).
- `lib/features/inventory/presentation/widgets/items_tab.dart` — سطران فقط:
  تمرير `itemId` و`onViewAlternatives` عند التعديل ليعمل قسم البدائل.
- `lib/l10n/app_ar.arb` و`app_en.arb` (+ الملفات المولّدة) — 9 مفاتيح جديدة
  لعناوين الأقسام السبعة وزر «عرض البدائل» وملاحظة ما بعد الحفظ.
- `test/item_form_ux_test.dart` — أُعيدت كتابته للتصميم الجديد (14 اختبارًا).
- `test/responsive_dialogs_test.dart` — أُعيدت كتابته: 4 أحجام شاشة + سلوك
  كامل (22 اختبارًا).
- `test/item_batch_purchase_flow_regression_test.dart` — إصلاح مساعد النقر
  ليعمل مع `Dialog` بدل `AlertDialog` فقط.

### 2. التغييرات في الواجهة
- **نافذة عمل واحدة كبيرة** بدل وضعي «إدخال سريع/مفصّل» والتبويبات الأربعة:
  - **يمين:** معلومات أساسية (الاسم التجاري عربي/إنجليزي، الباركودان) +
    التفاصيل الفنية (الاسم العلمي، الدواء المكافئ، الشكل الصيدلاني، الجرعة،
    الشركة المصنعة، الحجم، مكان الرف).
  - **وسط:** التعبئة والمبيعات (التعبئة التجارية، الأجزاء، عدد الأجزاء،
    مفتاح البيع الجزئي + نسبته + سعر الجزء) — التسعير (التكلفة، سعر البيع) —
    المخزون والتسعير المتقدم (الخصم، السعران المخصصان، الضريبة، الجملة،
    نصف الجملة، الحدين الأدنى/الأقصى، 4 مفاتيح، الموردون).
  - **يسار:** التصنيفات والاستطبابات (التصنيف، الاستطبابات، المواد الفعالة
    مع العيار لكل مادة) + **البدائل**.
- **رأس ثابت** (العنوان + زر إغلاق) و**تذييل ثابت** (إلغاء | حفظ و اضافة الى
  المخزون | حفظ) — لا يتحركان أبدًا.
- **لا تمرير للنافذة الرئيسية إطلاقًا**؛ كل عمود يمرّر داخليًا فقط إذا ضاق
  الارتفاع (شبكة أمان، لا تُستخدم في الأحجام المكتبية).
- **الشاشات الضيقة (<960px):** المجموعات الثلاث تُبدَّل بزر مقسّم
  (معلومات أساسية / التعبئة والمبيعات / التصنيفات والاستطبابات)، وأزرار
  التذييل تُرصّ في صفين مضغوطين — بلا تبويبات، بلا معالج، بلا تنقل جديد.
- Material 3 ودزاين سيستم المشروع نفسه (`CompactSection`، `FormGrid`،
  الحقول القابلة للبحث) وRTL كامل.

### 3. الحقول المحتفظ بها (كلها ظاهرة كما كانت)
الاسم التجاري (عربي/إنجليزي)، الباركود الأساسي/الثانوي (مع زر المسح)، الاسم
العلمي، الدواء المكافئ، الشكل الصيدلاني، الجرعة، الشركة المصنعة، الحجم، مكان
الرف، التعبئة التجارية، الأجزاء، عدد الأجزاء، البيع الجزئي (مفتاح/نسبة/سعر
الجزء/استعادة التلقائي)، التكلفة، سعر البيع، الخصم، السعران المخصصان، الضريبة،
سعر الجملة، سعر نصف الجملة، الحد الأدنى/الأقصى للمخزون، المفاتيح الأربعة (له
تاريخ صلاحية، دواء مراقب، قفل التحديث التلقائي للأسعار، يتطلب وصفة)، الموردون،
التصنيف، الاستطبابات، المواد الفعالة مع العيار.

### 4. الحقول المحذوفة من الواجهة (3 فقط، بقرار تصميمي)
- تعليمات الاستخدام، ملاحظات عامة، رقم الترخيص — كانت في تبويب «ملاحظات»
  القديم. حُذفت من العرض فقط لأنها لا تُستخدم في سير العمل اليومي للنافذة.

### 5. الحفاظ على بيانات قاعدة البيانات
- الحقول الثلاثة المحذوفة من الواجهة **تبقى في `ItemDraft` وتُمرَّر كما هي
  من القيم الأولية عند الحفظ** — الحذف من العرض ≠ حذف من القاعدة.
- اختبار انحدار مخصص (`retired UI fields keep their stored data on save`)
  يثبت أن القيم المخزنة تنجو من الحفظ سليمة.

### 6. المنطق التجاري غير المتغيّر
- صفر تغيير في: schema/الجداول/الترحيلات، المستودعات/DAOs/المتحكمات،
  التحقق (`_validate`)، الحفظ/التحديث (`_submit`)، تحويل التكلفة
  (`package_cost.dart`)، حسابات الأسعار والمخزون وFEFO والباركود والمواد
  الفعالة والموردين، وخوارزمية البدائل (القسم يفتح نافذة البدائل الحالية
  كما هي دون أي تعديل عليها).
- التعديل الوحيد خارج الواجهة: سطرا `itemId`/`onViewAlternatives` في
  `items_tab.dart` لتمرير معرّف الصنف — لا يغيّر أي سلوك حفظ/تنقل.

### 7. نتائج الفحص والاختبارات
- `flutter analyze`: **نظيف** (المشروع كاملًا).
- `flutter test`: **730 اختبارًا ناجحًا** (712 سابقًا + 6 جديدة في ملف UX +
  12 جديدة في ملف الاستجابة).
- تغطية الاستجابة الجديدة: لا فيض (overflow) في الأحجام الأربعة
  (1280×800، 1280×900، 1920×1080، 390×844)، لا تمرير للنافذة الرئيسية،
  التذييل ثابت وداخل الشاشة دائمًا، الحقول الأساسية reachable، RTL،
  إضافة/تعديل، التحقق، حفظ، حفظ وإضافة للمخزون، إلغاء، القيم الأولية تظهر
  في التعديل والمعدّلة تُحفظ.

### 8. القيود والملاحظات الصادقة
- **البدائل للعرض فقط**: النموذج الحالي (`ItemDraft`) لا يملك أي حقل علاقات
  بدائل، فلا يوجد «تحرير بدائل» داخل النافذة — القسم يفتح نافذة البدائل
  الموجودة (وضع التعديل) أو يعرض ملاحظة «تظهر البدائل بعد حفظ الصنف» (وضع
  الإنشاء). لم نخترع أي منطق بدائل جديد التزامًا بمنع تغيير السلوك.
- **المقارنة مع الصورة المرجعية**: التخطيط العام مطابق (رأس ثابت، 3 أعمدة،
  تذييل بثلاثة أزرار)، لكن التفاصيل الدقيقة (ألوان/أيقونات/نصوص الصورة)
  طُبّقت بروح نظام التصميم الحالي لا نسخًا بكسل-بكسل؛ التسميات الرسمية
  الحالية للمشروع حُوفظ عليها بدل نصوص الصورة حيث اختلفت.
- على الشاشات الضيقة جدًا يُستخدم مبدّل مقسّم — ليس تبويبًا بالمعنى
  القديم (لا صفحات منفصلة ولا خطوات)، لكنه يبقى عنصر تبديل إضافي فرضه
  ضيق العرض.
- لم يُختبر على جهاز علي الحقيقي بعد — بانتظار تأكيده.

**Commit:** `1b5ad09` — CI: يُتحقق بعد الدفع.
